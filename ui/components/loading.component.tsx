import React from 'react'
import { Box } from 'native-base'
import { styles } from '../../constants/styles'

export const LoadingScreen = () => {
	return <Box style={styles.container}>Loading...</Box>
}
